# Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# API-Football (https://www.api-football.com — free tier: 100 req/day)
API_FOOTBALL_KEY=your_api_football_key_here

# Encryption key for storing user Bayse API keys at rest (32 char random string)
ENCRYPTION_KEY=your_32_char_random_encryption_key

# Server
PORT=3001

# REQUIRED on Render — set this to your deployed service URL
# Enables self-ping every 14 min to prevent Render free tier sleep
# e.g. https://harbinger.onrender.com
RENDER_EXTERNAL_URL=https://your-service-name.onrender.com

# Trading engine defaults (users can override per account)
DEFAULT_CONFIDENCE_THRESHOLD=0.72
DEFAULT_MAX_TRADE_USD=5
SIGNAL_WARMUP_ALERT_THRESHOLD=0.60
ENGINE_TICK_INTERVAL_MS=60000
